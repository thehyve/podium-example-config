# ===================================================================
# Podium Sample Spring Cloud Config.
# ===================================================================

# Property used on app startup to check the config server status
configserver:
    name: Podium Registry config server
    status: Connected to the Podium Registry example config server, serving configuration from git!

podium:
    security:
        authentication:
            jwt:
                secret: change-me-in-production
